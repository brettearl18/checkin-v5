// Quick script to check onboarding data structure
const admin = require('firebase-admin');

// This is just to understand the structure - we'll check via API instead
console.log('To check onboarding data, we should query the API or Firestore directly');
